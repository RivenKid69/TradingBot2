# Licensing

This repository (`ccea-agent`) is licensed under **Apache-2.0** (see `LICENSE`).

It is part of an open-core split:
- **Public OSS:** `ccea-sdk` + `ccea-agent` (auditable trust layer + local enforcement)
- **Private:** Cloud/orchestration and all competitive trading/ML IP

