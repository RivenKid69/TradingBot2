Created At: 2026-05-31T09:27:17Z
Completed At: 2026-05-31T09:27:17Z
The following changes were made by the replace_file_content tool to: c:\Users\Зефирка\StartUp\index.html. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -3006,7 +3006,7 @@
 
         function getWorkflowProgress(asset, mode) {
             try {
-                const data = JSON.parse(localStorage.getItem('rivenquant_workflow_progress') || '{}');
+                const data = JSON.parse(localStorage.getItem('rivenquant_workflow_progress_v3') || '{}');
                 if (!data[asset]) data[asset] = { ai: 0, custom: 0 };
                 
                 if (data[asset][mode] === undefined) {
@@ -3013,4 +3013,4 @@
-                    localStorage.setItem('rivenquant_workflow_progress', JSON.stringify(data));
+                    localStorage.setItem('rivenquant_workflow_progress_v3', JSON.stringify(data));
                 }
                 
                 return data[asset][mode];
@@ -3020,13 +3020,21 @@
 
         function saveWorkflowProgress(asset, mode, step) {
             try {
-                const data = JSON.parse(localStorage.getItem('rivenquant_workflow_progress') || '{}');
+                const data = JSON.parse(localStorage.getItem('rivenquant_workflow_progress_v3') || '{}');
                 if (!data[asset]) data[asset] = { ai: 0, custom: 0 };
                 data[asset][mode] = step;
-                localStorage.setItem('rivenquant_workflow_progress', JSON.stringify(data));
+                localStorage.setItem('rivenquant_workflow_progress_v3', JSON.stringify(data));
             } catch (e) {}
         }
 
+        function resetWorkflowProgress() {
+            if (confirm("Вы уверены, что хотите сбросить прогресс по этому активу?")) {
+                saveWorkflowProgress(activeAsset, quickStartMode, 0);
+                renderWorkflowTimeline(activeAsset);
+                showActionToast("Прогресс сброшен.");
+            }
+        }
+
         function goBackToAssetSelection() {
             const panel = document.getElementById('lite-workflow-panel');
             const welcome = document.getElementById('lite-welcome-header');
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.
