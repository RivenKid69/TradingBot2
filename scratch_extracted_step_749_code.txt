"        function renderPipelineStatusDetails(asset) {\n            const statusCard = document.getElementById('workflow-pipeline-status');\n            const detailsContainer = document.getElementById('workflow-pipeline-steps-details');\n            const overallStatusBadge = document.getElementById('workflow-pipeline-overall-status');\n            if (!statusCard || !detailsContainer) return;\n\n            const mode = quickStartMode;\n            const currentStep = getWorkflowProgress(asset, mode);\n            const steps = mode === 'ai' ? aiSteps : customSteps;\n\n            if (currentStep === 0 && !currentRunningJob) {\n                // Not started yet and nothing is running\n                overallStatusBadge.innerText = 'ИНИЦИАЛИЗАЦИЯ';\n                overallStatusBadge.className = 'text-[9px] px-1.5 py-0.5 rounded bg-blue-500/10 text-blue-400 border border-blue-500/20 font-bold';\n                \n                detailsContainer.innerHTML = `\n                    <div class=\"flex flex-col items-center justify-center py-6 text-center space-y-2\">\n                        <i class=\"fa-solid fa-hourglass-start text-gray-600 text-lg animate-pulse\"></i>\n                        <div class=\"text-[11px] text-gray-400 font-medium font-sans\">Ожидание запуска проекта</div>\n                        <p class=\"text-[9px] text-gray-500 leading-normal max-w-[200px] font-sans\">\n                            Настройте и запустите <strong>Шаг 1: ${steps[0].title}</strong>, чтобы загрузить данные и запустить телеметрию.\n                        </p>\n                    </div>\n                `;\n                statusCard.classList.remove('hidden');\n                return;\n            }\n\n            // Overall Status Label\n            if (currentRunningJob) {\n                const runningIdx = steps.findIndex(s => s.job === currentRunningJob);\n                overallStatusBadge.innerText = `ВЫПОЛН
<truncated 35370 bytes>